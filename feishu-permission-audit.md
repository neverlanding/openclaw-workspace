# 飞书应用权限审计报告

**生成时间**: 2026-02-27 08:17 GMT+8  
**审计对象**: 飞书机器人应用权限  
**已授予权限数量**: 58 项

---

## 一、用户需求概述

| 序号 | 功能需求 | 描述 |
|------|----------|------|
| 1 | 主动发送图片给用户 | 机器人能够主动向用户发送图片消息 |
| 2 | 主动发起聊天（单聊） | 机器人能够主动创建与用户的单聊会话 |
| 3 | 与指定的其他联系人聊天 | 机器人能够与指定联系人进行消息交互 |
| 4 | 在群聊里聊天 | 机器人能够在群聊中发送和接收消息 |

---

## 二、权限分析详情

### 2.1 需求 1：主动发送图片给用户 🔴 **部分满足**

#### 已具备权限：
- ✅ `im:message` - 基础消息权限
- ✅ `im:message:send_as_bot` - 以机器人身份发送消息
- ✅ `im:message:send_multi_users` - 向多个用户发送消息
- ✅ `im:resource` - 资源访问权限（用于上传图片）

#### 缺失权限：
- ❌ **`im:message:send`** - 主动发送消息的核心权限（最关键）
- ❌ **`im:message:image`** 或 **`im:message:upload_image`** - 发送图片的专用权限（如存在）

#### 分析说明：
当前权限列表中有 `im:message` 和 `im:message:send_as_bot`，但**缺少明确的 `im:message:send` 权限**。在飞书权限体系中：
- `im:message:send_as_bot` 通常用于在已存在的会话中以机器人身份回复
- `im:message:send` 才是主动发起消息发送的核心权限
- `im:resource` 允许访问资源，但发送图片需要先上传再发送，需要完整的发送权限

#### 建议操作：
添加 `im:message:send` 权限以支持主动发送消息（包括图片）。

---

### 2.2 需求 2：主动发起聊天（单聊） 🟢 **基本满足**

#### 已具备权限：
- ✅ `im:chat:create` - 创建聊天会话
- ✅ `im:chat` - 基础聊天权限
- ✅ `contact:user.base:readonly` - 读取用户基本信息
- ✅ `contact:contact.base:readonly` - 读取联系人基本信息
- ✅ `contact:contact.base:readonly(user)` - 读取用户联系人信息

#### 缺失权限：
- ⚠️ **`im:chat:manage`** 或 **`im:chat:p2p`** - 可能需要额外的单聊管理权限（取决于具体实现）

#### 分析说明：
当前已具备创建聊天会话 (`im:chat:create`) 和读取联系人信息的基础权限，理论上可以发起单聊。但需要注意：
- 飞书机器人主动发起单聊可能受到**用户隐私设置**限制
- 某些场景下可能需要 `im:chat.access_event.bot_p2p_chat:read`（已有）来接收单聊事件

#### 建议操作：
当前权限基本满足，如遇问题可考虑添加 `im:chat:manage` 增强单聊管理能力。

---

### 2.3 需求 3：与指定的其他联系人聊天 🟡 **部分满足**

#### 已具备权限：
- ✅ `contact:contact.base:readonly` - 读取联系人基本信息
- ✅ `im:message:send_multi_users` - 向多用户发送消息
- ✅ `im:message:send_as_bot` - 以机器人身份发送消息
- ✅ `im:chat.access_event.bot_p2p_chat:read` - 读取机器人单聊事件

#### 缺失权限：
- ❌ **`im:message:send`** - 主动发送消息权限（同需求 1）
- ❌ **`contact:user.mobile:readonly`** - 如需通过手机号查找联系人
- ❌ **`contact:user.email:readonly`** - 如需通过邮箱查找联系人

#### 分析说明：
读取联系人信息的权限已具备，但**发送消息的核心权限 `im:message:send` 缺失**。这意味着：
- 可以读取联系人列表
- 可以在收到消息后回复
- 但**无法主动向指定联系人发起消息**

#### 建议操作：
添加 `im:message:send` 权限以支持主动联系指定联系人。

---

### 2.4 需求 4：在群聊里聊天 🟢 **满足**

#### 已具备权限：
- ✅ `im:message.group_msg` - 发送群消息
- ✅ `im:message.group_at_msg:readonly` - 读取群@消息
- ✅ `im:chat:read` - 读取聊天信息
- ✅ `im:chat:readonly` - 只读聊天权限
- ✅ `im:chat.members:read` - 读取群成员
- ✅ `im:chat.members:bot_access` - 机器人访问群成员

#### 缺失权限：
- 无明显缺失

#### 分析说明：
群聊相关权限较为完整，包括：
- 发送群消息 (`im:message.group_msg`)
- 读取群信息 (`im:chat:read`, `im:chat:readonly`)
- 管理群成员 (`im:chat.members:read`)
- 群公告、群置顶等高级功能也有相应权限

#### 建议操作：
当前权限已满足群聊基础功能，无需额外添加。

---

## 三、权限缺口总结

### 3.1 关键缺失权限（必须添加）

| 权限代码 | 用途 | 影响需求 | 优先级 |
|----------|------|----------|--------|
| `im:message:send` | 主动发送消息 | 需求 1、3 | 🔴 P0 |
| `im:message:send_multi_depts` | 向多部门发送（已有但需确认） | 需求 1、3 | 🟡 P1 |

### 3.2 建议补充权限（可选增强）

| 权限代码 | 用途 | 影响需求 | 优先级 |
|----------|------|----------|--------|
| `contact:user.mobile:readonly` | 通过手机号查找用户 | 需求 3 | 🟢 P2 |
| `contact:user.email:readonly` | 通过邮箱查找用户 | 需求 3 | 🟢 P2 |
| `im:chat:manage` | 增强单聊管理能力 | 需求 2 | 🟢 P2 |
| `im:message:upload_image` | 上传图片专用权限（如存在） | 需求 1 | 🟢 P2 |

---

## 四、当前权限完整列表

### 4.1 消息相关权限 (im:message)
```
im:message
im:message:send_as_bot
im:message:send_multi_users
im:message:send_multi_depts
im:message:send_sys_msg
im:message.group_msg
im:message.group_at_msg:readonly
im:message.p2p_msg:readonly
im:message:readonly
im:message:update
im:message:recall
im:message.pins:read
im:message.pins:write_only
im:message.reactions:read
im:message.reactions:write_only
im:message.urgent
im:message.urgent:phone
im:message.urgent:sms
im:message.urgent.status:write
```

### 4.2 聊天相关权限 (im:chat)
```
im:chat
im:chat:read
im:chat:readonly
im:chat:create
im:chat:update
im:chat:delete
im:chat:operate_as_owner
im:chat:moderation:write_only
im:chat.members:read
im:chat.members:write_only
im:chat.members:bot_access
im:chat.managers:write_only
im:chat.widgets:read
im:chat.widgets:write_only
im:chat.collab_plugins:read
im:chat.collab_plugins:write_only
im:chat.tabs:read
im:chat.tabs:write_only
im:chat.menu_tree:read
im:chat.menu_tree:write_only
im:chat.announcement:read
im:chat.announcement:write_only
im:chat.top_notice:write_only
im:chat.chat_pins:read
im:chat.chat_pins:write_only
im:chat.access_event.bot_p2p_chat:read
im:chat.moderation:read
```

### 4.3 联系人相关权限 (contact)
```
contact:user.base:readonly
contact:contact.base:readonly
contact:contact.base:readonly(user)
```

### 4.4 其他权限
```
im:tag:read
im:tag:write
im:biz_entity_tag_relation:read
im:biz_entity_tag_relation:write
im:url_preview.update
im:resource
im:app_feed_card:write
im:datasync.feed_card.time_sensitive:write
im:user_agent:read
```

---

## 五、操作建议

### 5.1 立即执行（P0）

1. **添加 `im:message:send` 权限**
   - 前往飞书开放平台 → 应用管理 → 权限管理
   - 搜索并添加 `im:message:send` 权限
   - 提交审核（如需）
   - 重新发布应用

### 5.2 后续优化（P1/P2）

2. **验证图片发送功能**
   - 测试上传和发送图片的完整流程
   - 如遇问题，检查是否需要额外的图片上传权限

3. **增强联系人查找能力**（如需要）
   - 添加 `contact:user.mobile:readonly`
   - 添加 `contact:user.email:readonly`

4. **完善单聊管理**（如需要）
   - 考虑添加 `im:chat:manage` 权限

---

## 六、风险提示

### 6.1 权限审核
- 部分权限（如 `im:message:send`）可能需要飞书官方审核
- 审核时间通常为 1-3 个工作日
- 提前准备应用场景说明

### 6.2 用户隐私
- 主动发送消息需遵守飞书平台规范
- 避免频繁骚扰用户
- 提供退订/免打扰机制

### 6.3 权限最小化原则
- 仅申请实际需要的权限
- 定期审查已授权权限
- 及时移除不再使用的权限

---

## 七、结论

| 需求 | 状态 | 关键缺失 |
|------|------|----------|
| 1. 主动发送图片给用户 | 🔴 部分满足 | `im:message:send` |
| 2. 主动发起聊天（单聊） | 🟢 基本满足 | 无关键缺失 |
| 3. 与指定的其他联系人聊天 | 🔴 部分满足 | `im:message:send` |
| 4. 在群聊里聊天 | 🟢 满足 | 无关键缺失 |

**总体评估**: 当前权限配置**部分满足**用户需求，核心缺失为 `im:message:send` 权限。添加该权限后，4 项需求均可得到较好支持。

---

*报告生成完成。如有疑问，请参考飞书开放平台官方文档或联系飞书技术支持。*
